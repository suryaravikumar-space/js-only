/**
 * Question 10: Character Frequency Counter
 *
 * Task: Write a function that counts the frequency of each character
 * in a string and returns an object with the counts.
 *
 * Examples:
 * - "hello" -> { h: 1, e: 1, l: 2, o: 1 }
 * - "aabbcc" -> { a: 2, b: 2, c: 2 }
 *
 * Bonus: Find the most/least frequent character
 */

function charFrequency(str) {
    // Your solution here
}

function mostFrequentChar(str) {
    // Bonus: Your solution here
}

// Test cases
console.log(charFrequency("hello")); // { h: 1, e: 1, l: 2, o: 1 }
console.log(charFrequency("aabbcc")); // { a: 2, b: 2, c: 2 }
console.log(mostFrequentChar("hello")); // "l"
console.log(mostFrequentChar("aabbbcc")); // "b"
