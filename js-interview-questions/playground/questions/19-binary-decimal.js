/**
 * Question 19: Binary to Decimal & Decimal to Binary
 *
 * Task: Write functions to convert:
 * 1. Decimal to Binary
 * 2. Binary to Decimal
 *
 * Examples:
 * - decToBin(10) -> "1010"
 * - binToDec("1010") -> 10
 * - decToBin(255) -> "11111111"
 *
 * Note: Try without using built-in methods
 */

function decimalToBinary(num) {
    // Your solution here (without toString(2))
}

function binaryToDecimal(binary) {
    // Your solution here (without parseInt(bin, 2))
}

// Test cases
console.log(decimalToBinary(10)); // "1010"
console.log(decimalToBinary(255)); // "11111111"
console.log(binaryToDecimal("1010")); // 10
console.log(binaryToDecimal("11111111")); // 255
